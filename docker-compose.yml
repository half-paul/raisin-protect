# Raisin Protect — Docker Compose
# Ports: postgres=5433, redis=6380, api=8090, dashboard=3010
# (Non-default ports to avoid conflicts with Raisin Shield)

services:
  postgres:
    image: postgres:16-alpine
    container_name: rp-postgres
    environment:
      POSTGRES_USER: rp
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rp_dev_password}
      POSTGRES_DB: raisin_protect
    ports:
      - "5434:5432"
    volumes:
      - rp_postgres_data:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d/migrations
      - ./db/seeds:/docker-entrypoint-initdb.d/seeds
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rp -d raisin_protect"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - rp-net
    deploy:
      resources:
        limits:
          memory: 512M

  redis:
    image: redis:7-alpine
    container_name: rp-redis
    ports:
      - "6380:6379"
    volumes:
      - rp_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - rp-net
    deploy:
      resources:
        limits:
          memory: 128M

  minio:
    image: minio/minio:latest
    container_name: rp-minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-rp-admin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-changeme-minio}
    volumes:
      - rp_minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - rp-net
    deploy:
      resources:
        limits:
          memory: 256M

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: rp-api
    ports:
      - "8090:8090"
    environment:
      RP_PORT: "8090"
      RP_ENV: ${RP_ENV:-development}
      RP_DB_URL: postgres://rp:${POSTGRES_PASSWORD:-rp_dev_password}@postgres:5432/raisin_protect?sslmode=disable
      RP_REDIS_URL: redis://redis:6379
      RP_JWT_SECRET: ${RP_JWT_SECRET:-dev-jwt-secret-change-in-production}
      RP_JWT_ACCESS_TTL: ${RP_JWT_ACCESS_TTL:-15m}
      RP_JWT_REFRESH_TTL: ${RP_JWT_REFRESH_TTL:-168h}
      RP_BCRYPT_COST: ${RP_BCRYPT_COST:-12}
      RP_CORS_ORIGINS: ${RP_CORS_ORIGINS:-http://localhost:3010}
      RP_LOG_LEVEL: ${RP_LOG_LEVEL:-info}
      RP_MINIO_ENDPOINT: minio:9000
      RP_MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-rp-admin}
      RP_MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-changeme-minio}
      RP_MINIO_BUCKET: rp-evidence
      RP_MINIO_USE_SSL: "false"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8090/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - rp-net
    deploy:
      resources:
        limits:
          memory: 256M

  # Dashboard placeholder — Frontend Developer will populate this
  # dashboard:
  #   build:
  #     context: ./dashboard
  #     dockerfile: Dockerfile
  #   container_name: rp-dashboard
  #   ports:
  #     - "3010:3000"
  #   environment:
  #     NEXT_PUBLIC_API_URL: http://localhost:8090
  #   depends_on:
  #     api:
  #       condition: service_healthy
  #   networks:
  #     - rp-net

volumes:
  rp_postgres_data:
  rp_redis_data:
  rp_minio_data:

networks:
  rp-net:
    driver: bridge
